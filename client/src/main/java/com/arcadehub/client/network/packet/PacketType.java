
package com.arcadehub.client.network.packet;

public enum PacketType {
    JOIN_LOBBY,
    STATE_UPDATE,
    INPUT,
    CHAT,
    HEARTBEAT,
    READY,
    LEADERBOARD_REQUEST,
    LEADERBOARD_RESPONSE,
    LOBBY_UPDATE
}
